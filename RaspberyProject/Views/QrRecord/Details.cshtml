@model BLL.DTOs.QrRecordDtos.QrRecordDetailDto

@{
    ViewData["Title"] = "QR Kayıt Detayı";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">

            <div class="card shadow-sm border-0">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0">📄 QR Kayıt Detayı</h5>
                </div>

                <div class="card-body">

                    <dl class="row">
                        <dt class="col-sm-4">Cihaz Adı</dt>
                        <dd class="col-sm-8">@Model.DeviceName</dd>

                        <dt class="col-sm-4">İçerik</dt>
                        <dd class="col-sm-8">@Model.Content</dd>

                        <dt class="col-sm-4">Taranma Tarihi</dt>
                        <dd class="col-sm-8">@Model.ScannedAt.ToString("dd.MM.yyyy HH:mm")</dd>

                        <dt class="col-sm-4">QR Görsel</dt>
                        <dd class="col-sm-8">
                            <img src="@Url.Action("QrImage", "QrRecord", new { id = Model.Id })"
                                 alt="QR Kod" style="height: 128px;" class="img-thumbnail" />
                        </dd>
                    </dl>

                    <div class="d-flex justify-content-end">
                        <a asp-action="Index" class="btn btn-outline-primary">🔙 Geri Dön</a>
                    </div>

                </div>
            </div>

        </div>
    </div>
</div>



@* @model BLL.DTOs.QrRecordDtos.QrRecordDetailDto

@{
    ViewData["Title"] = "QR Kayıt Detayı";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">

            <div class="card shadow-sm border-0">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0">📄 QR Kayıt Detayı</h5>
                </div>

                <div class="card-body">

                    <dl class="row">
                        <dt class="col-sm-4">Cihaz Adı</dt>
                        <dd class="col-sm-8">@Model.DeviceName</dd>

                        <dt class="col-sm-4">İçerik</dt>
                        <dd class="col-sm-8">@Model.Content</dd>

                        <dt class="col-sm-4">Taranma Tarihi</dt>
                        <dd class="col-sm-8">@Model.ScannedAt.ToString("dd.MM.yyyy HH:mm")</dd>
                    </dl>

                    <div class="d-flex justify-content-end">
                        <a asp-action="Index" class="btn btn-outline-primary">🔙 Geri Dön</a>
                    </div>

                </div>
            </div>

        </div>
    </div>
</div>
 *@